<!DOCTYPE html>
<html>
  <head>
    <title>Full (Stack!) House</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <%# <link href="https://fonts.googleapis.com/css?family=Arimo|Cabin" rel="stylesheet"> %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Libre+Franklin&display=swap" rel="stylesheet">
  </head>

  <body>
  
    <% if params[:controller] == 'home' %>
      <%= video_tag "/images/headers/Full (Stack!) House.mp4", autoplay: :autoplay, muted: :muted, class: "header-vid" %>
    <% else %>
      <header class="header">
      </header>
    <% end %>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top" style="background-color: #e3f2fd;">
      <div class="navbar-nav mr-auto">
        <%= link_to 'Home', root_path, class: "nav-link" %>
        <%= link_to 'Card Library', cards_path, class: "nav-link" %>
        <%= link_to 'Deck Vault', decks_path, class: "nav-link" %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>